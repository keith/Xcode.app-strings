@(#)PROGRAM:xcdebug  PROJECT:IDEFrameworks-23252.3
3333
Error: %@
xcdebug -- Start a debugging session in Xcode.
There are two ways to use xcdebug:
(1) xcdebug [options] <command> [args ...]
    Starts a 'process-based' debugging session: Spawns the given command with
    the given arguments in the current terminal and asks the most recently used
    Xcode window to attach to it (opens a new temporary debugging-only window if
    no windows are open).
(2) xcdebug -s <name> [options] [args ...]
    Starts a 'scheme-based' debugging session: Triggers the 'Run Without
    Building' action in Xcode using the given scheme name. The process
    to run is defined by the scheme. If arguments are specified, they are
    appended as extra arguments when launching.
Options (general):
    -h, --help                Show this information.
    -v, --version             Print version information.
    -p, --pid <pid>           Target Xcode with process id <pid>.
    -x, --xcode-path <path>   Target Xcode from path <path>.
    -w, --workspace <name>    Target workspace named <name>.
    -b, --background          Leave Xcode as a background app.
Options (process-based):
    -S, --suspended           Start debugging in a suspended state.
    -t, --temporary-workspace Always create a new temporary workspace.
Options (scheme-based):
    -s, --scheme <name>       Launch target using the scheme <name>.
    -d, --destination <spec>  Use a run destination matching <spec>.
    -B, --build               Perform 'Run' instead of 'Run Without Building'.
    -e, --environment <env>   Use an extra environment variable of NAME=VALUE.
                              This flag can be used multiple times.
--help
--version
xcdebug version %s
--pid
--xcode-path
--workspace
--background
--suspended
--temporary-workspace
--scheme
--destination
--build
--environment
invalid option: %s
posix_spawnattr_init
posix_spawnattr_setflags
posix_spawnp failed with error %d
pid is zero???
pid %d isn't a running Xcode
Specified workspace '%s' is not open.
Currently open workspaces:
  %s
Timed out creating a temporary workspace.
waitpid(%d) failed with error %d
../../../Info.plist
Xcode.app/Contents/Info.plist
CFBundleShortVersionString
com.apple.dt.Xcode
XCODE_DEVELOPER_DIR
Developer
URLByDeletingLastPathComponent
URLByAppendingPathComponent:isDirectory:
setLaunchFlags:
UTF8String
activate
activeWorkspaceDocument
addObject:
applicationWithBundleIdentifier:
applicationWithProcessIdentifier:
applicationWithURL:
arguments
attachToProcessIdentifier:suspended:
count
countByEnumeratingWithState:objects:count:
createTemporaryDebuggingWorkspace
currentDirectoryPath
debugScheme:runDestinationSpecifier:skipBuilding:commandLineArguments:environmentVariables:
defaultManager
description
dictionaryWithContentsOfURL:error:
environment
eventDidFail:withError:
executableURL
fileURLWithPath:
fileURLWithPath:isDirectory:
fileURLWithPathComponents:
firstObject
isAbsolutePath
isEqualToString:
isRunning
lastObject
loaded
mainBundle
name
objectForKeyedSubscript:
pathComponents
pathExtension
processInfo
setDelegate:
stringWithUTF8String:
subarrayWithRange:
timeIntervalSinceDate:
workspaceDocuments
TedObserver
SBApplicationDelegate
@32@0:8r^{AEDesc=I^^{OpaqueAEDataStorageType}}16@24
@32@0:8r^{AEDesc=I^^{OpaqueAEDataStorageType}}16@"NSError"24
@(#)PROGRAM:xcdebug  PROJECT:IDEFrameworks-23252.3
3333
Error: %@
xcdebug -- Start a debugging session in Xcode.
There are two ways to use xcdebug:
(1) xcdebug [options] <command> [args ...]
    Starts a 'process-based' debugging session: Spawns the given command with
    the given arguments in the current terminal and asks the most recently used
    Xcode window to attach to it (opens a new temporary debugging-only window if
    no windows are open).
(2) xcdebug -s <name> [options] [args ...]
    Starts a 'scheme-based' debugging session: Triggers the 'Run Without
    Building' action in Xcode using the given scheme name. The process
    to run is defined by the scheme. If arguments are specified, they are
    appended as extra arguments when launching.
Options (general):
    -h, --help                Show this information.
    -v, --version             Print version information.
    -p, --pid <pid>           Target Xcode with process id <pid>.
    -x, --xcode-path <path>   Target Xcode from path <path>.
    -w, --workspace <name>    Target workspace named <name>.
    -b, --background          Leave Xcode as a background app.
Options (process-based):
    -S, --suspended           Start debugging in a suspended state.
    -t, --temporary-workspace Always create a new temporary workspace.
Options (scheme-based):
    -s, --scheme <name>       Launch target using the scheme <name>.
    -d, --destination <spec>  Use a run destination matching <spec>.
    -B, --build               Perform 'Run' instead of 'Run Without Building'.
    -e, --environment <env>   Use an extra environment variable of NAME=VALUE.
                              This flag can be used multiple times.
--help
--version
xcdebug version %s
--pid
--xcode-path
--workspace
--background
--suspended
--temporary-workspace
--scheme
--destination
--build
--environment
invalid option: %s
posix_spawnattr_init
posix_spawnattr_setflags
posix_spawnp failed with error %d
pid is zero???
pid %d isn't a running Xcode
Specified workspace '%s' is not open.
Currently open workspaces:
  %s
Timed out creating a temporary workspace.
waitpid(%d) failed with error %d
../../../Info.plist
Xcode.app/Contents/Info.plist
CFBundleShortVersionString
com.apple.dt.Xcode
XCODE_DEVELOPER_DIR
Developer
TedObserver
SBApplicationDelegate
URLByDeletingLastPathComponent
URLByAppendingPathComponent:isDirectory:
setLaunchFlags:
UTF8String
activate
activeWorkspaceDocument
addObject:
applicationWithBundleIdentifier:
applicationWithProcessIdentifier:
applicationWithURL:
arguments
attachToProcessIdentifier:suspended:
count
countByEnumeratingWithState:objects:count:
createTemporaryDebuggingWorkspace
currentDirectoryPath
debugScheme:runDestinationSpecifier:skipBuilding:commandLineArguments:environmentVariables:
defaultManager
description
dictionaryWithContentsOfURL:error:
environment
eventDidFail:withError:
executableURL
fileURLWithPath:
fileURLWithPath:isDirectory:
fileURLWithPathComponents:
firstObject
isAbsolutePath
isEqualToString:
isRunning
lastObject
loaded
mainBundle
name
objectForKeyedSubscript:
pathComponents
pathExtension
processInfo
setDelegate:
stringWithUTF8String:
subarrayWithRange:
timeIntervalSinceDate:
workspaceDocuments
@32@0:8r^{AEDesc=I^^{OpaqueAEDataStorageType}}16@24
@32@0:8r^{AEDesc=I^^{OpaqueAEDataStorageType}}16@"NSError"24
