@(#)PROGRAM:unifdef  PROJECT:developer_cmds-83
i:D:U:f:I:M:o:x:bBcdehKklmnsStV
-B and -b are mutually exclusive
-s only works with one input file
-o cannot be used with multiple input files
multiple input files require -m or -M
-m requires an input file
[stdin]
can't open %s
can't create %s
.XXXXXX
TMPDIR
/tmp/
%s/%s
asprintf
can't rename "%s" to "%s"
process line %d %s -> %s depth %d
ifdef
ifndef
elif
else
endif
can't read %s
parser insert newline at EOF
parser concatenate dangling whitespace
parser line %d state %s comment %s line
Unterminated char literal
Unterminated string literal
%s: %d: %s
%s: %d: %s (#if line %d depth %d)
Output may be truncated
%s: can't write to output
%s%.*s%s
findsym
%s %s%c%s
undef
eval %s
eval = %d
eval%d
eval%d %s
eval%d = %d
eval%d lt = %s
eval%d !
eval%d ~
eval%d -
eval%d (
eval%d number
defined
eval%d defined missing ')'
eval%d defined unknown
eval%d defined %s
eval%d symbol
eval%d bad expr
eval division by zero
Obfuscated preprocessor control line
STARTING
FINISHING
CHAR
STRING
START
HASH
DIRTY
Too many levels of nesting
Inappropriate #elif
Inappropriate #else
Inappropriate #endif
#line %d%s
#line %d "%s"%s
EOF in comment
Premature EOF
%s%s
TRUEI
FALSEI
TRUE
FALSE
ELIF
ELTRUE
ELFALSE
ELSE
ENDIF
DODGY TRUEI
DODGY FALSEI
DODGY IF
DODGY TRUE
DODGY FALSE
DODGY ELIF
DODGY ELTRUE
DODGY ELFALSE
DODGY ELSE
DODGY ENDIF
PLAIN
ERROR
OUTSIDE
FALSE_PREFIX
TRUE_PREFIX
PASS_MIDDLE
FALSE_MIDDLE
TRUE_MIDDLE
PASS_ELSE
FALSE_ELSE
TRUE_ELSE
FALSE_TRAILER
snprintf
malloc
@(#) $Version: unifdef-2.12 $
@(#) $Date: 2020-02-14 16:37:16 +0000 $
@(#) $Author: Tony Finch (dot@dotat.at) $
@(#) $URL: http://dotat.at/prog/unifdef $
usage:
unifdef [-bBcdehKkmnsStV] [-x{012}] [-Mext] [-opath] \
[-[i]Dsym[=val]] [-[i]Usym] [-fpath] ... [file] ...
indir...
...ectsym
addsym2
unifdef.c
r == NULL
addsym
updsym
define
Missing macro name in #define
#define
Missing macro name in #undef
#undef
Unrecognized preprocessor directive
-Dsym=val  define preprocessor symbol with given value
-Dsym      define preprocessor symbol with value 1
-Usym
   preprocessor symbol is undefined
-iDsym=val \  ignore C strings and comments
-iDsym      ) in sections controlled by these
-iUsym
   /  preprocessor symbols
-fpath
file containing #define and #undef directives
blank lines instead of deleting them
compress blank lines around deleted section
complement (invert) keep vs. delete
debugging mode
ignore multiline preprocessor directives
print help
-Ipath
extra include file path (ignored)
disable && and || short-circuiting
process constant #if expressions
-Mext
modify in place and keep backups
modify input files in place
add #line directives to output
-opath
output file name
list #if control symbols with nesting
list #if control symbols
ignore C strings and comments
print version
-x{012}
exit status mode
@(#)PROGRAM:unifdef  PROJECT:developer_cmds-83
39@EMX
i:D:U:f:I:M:o:x:bBcdehKklmnsStV
-B and -b are mutually exclusive
-s only works with one input file
-o cannot be used with multiple input files
multiple input files require -m or -M
-m requires an input file
[stdin]
can't open %s
can't create %s
.XXXXXX
TMPDIR
/tmp/
%s/%s
asprintf
can't rename "%s" to "%s"
process line %d %s -> %s depth %d
ifdef
ifndef
elif
else
endif
can't read %s
parser insert newline at EOF
parser concatenate dangling whitespace
parser line %d state %s comment %s line
Unterminated char literal
Unterminated string literal
%s: %d: %s
%s: %d: %s (#if line %d depth %d)
Output may be truncated
%s: can't write to output
%s%.*s%s
findsym
%s %s%c%s
undef
eval %s
eval = %d
eval%d
eval%d %s
eval%d = %d
eval%d lt = %s
eval%d !
eval%d ~
eval%d -
eval%d (
eval%d number
defined
eval%d defined missing ')'
eval%d defined unknown
eval%d defined %s
eval%d symbol
eval%d bad expr
eval division by zero
Obfuscated preprocessor control line
STARTING
FINISHING
CHAR
STRING
START
HASH
DIRTY
Too many levels of nesting
Inappropriate #elif
Inappropriate #else
Inappropriate #endif
#line %d%s
#line %d "%s"%s
EOF in comment
Premature EOF
%s%s
TRUEI
FALSEI
TRUE
FALSE
ELIF
ELTRUE
ELFALSE
ELSE
ENDIF
DODGY TRUEI
DODGY FALSEI
DODGY IF
DODGY TRUE
DODGY FALSE
DODGY ELIF
DODGY ELTRUE
DODGY ELFALSE
DODGY ELSE
DODGY ENDIF
PLAIN
ERROR
OUTSIDE
FALSE_PREFIX
TRUE_PREFIX
PASS_MIDDLE
FALSE_MIDDLE
TRUE_MIDDLE
PASS_ELSE
FALSE_ELSE
TRUE_ELSE
FALSE_TRAILER
snprintf
malloc
@(#) $Version: unifdef-2.12 $
@(#) $Date: 2020-02-14 16:37:16 +0000 $
@(#) $Author: Tony Finch (dot@dotat.at) $
@(#) $URL: http://dotat.at/prog/unifdef $
usage:
unifdef [-bBcdehKkmnsStV] [-x{012}] [-Mext] [-opath] \
[-[i]Dsym[=val]] [-[i]Usym] [-fpath] ... [file] ...
indir...
...ectsym
addsym2
unifdef.c
r == NULL
addsym
updsym
define
Missing macro name in #define
#define
Missing macro name in #undef
#undef
Unrecognized preprocessor directive
-Dsym=val  define preprocessor symbol with given value
-Dsym      define preprocessor symbol with value 1
-Usym
   preprocessor symbol is undefined
-iDsym=val \  ignore C strings and comments
-iDsym      ) in sections controlled by these
-iUsym
   /  preprocessor symbols
-fpath
file containing #define and #undef directives
blank lines instead of deleting them
compress blank lines around deleted section
complement (invert) keep vs. delete
debugging mode
ignore multiline preprocessor directives
print help
-Ipath
extra include file path (ignored)
disable && and || short-circuiting
process constant #if expressions
-Mext
modify in place and keep backups
modify input files in place
add #line directives to output
-opath
output file name
list #if control symbols with nesting
list #if control symbols
ignore C strings and comments
print version
-x{012}
exit status mode
